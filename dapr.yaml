version: 1
common:
  resourcesPath: ./resources
  logLevel: info
  appLogDestination: console
  daprdLogDestination: console

apps:
- appID: durable-agent-ui
  appDirPath: ./
  command: [ "chainlit", "run", "durable_agent_ui_workaround.py", "-w" ]

#  dapr run --app-id durable-agent-ui --resources-path ./resources -- chainlit run durable_agent_ui_workaround.py -w
